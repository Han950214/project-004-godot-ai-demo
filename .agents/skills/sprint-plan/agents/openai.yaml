interface:
  display_name: "sprint-plan"
  short_description: "Generates a new sprint plan or updates an existing one based on the current milestone, completed work, and available capacity. Pulls context from production documents and design ba"
  default_prompt: "使用 $sprint-plan 完成当前明确范围内的工作。"

policy:
  allow_implicit_invocation: true
