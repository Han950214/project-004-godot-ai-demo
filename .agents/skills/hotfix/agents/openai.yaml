interface:
  display_name: "hotfix"
  short_description: "Emergency fix workflow that bypasses normal sprint processes with a full audit trail. Creates hotfix branch, tracks approvals, and ensures the fix is backported correctly. 当用户明确点名该"
  default_prompt: "使用 $hotfix 完成当前明确范围内的工作。"

policy:
  allow_implicit_invocation: true
