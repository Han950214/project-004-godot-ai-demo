interface:
  display_name: "setup-engine"
  short_description: "Configure the project's game engine and version. Pins the engine in AGENTS.md, detects knowledge gaps, and populates engine reference docs via web search when the version is beyond"
  default_prompt: "使用 $setup-engine 完成当前明确范围内的工作。"

policy:
  allow_implicit_invocation: false
