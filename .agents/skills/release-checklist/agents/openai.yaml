interface:
  display_name: "release-checklist"
  short_description: "Generates a comprehensive pre-release validation checklist covering build verification, certification requirements, store metadata, and launch readiness. 当用户明确点名该流程或当前任务与其专业目标直接匹配时"
  default_prompt: "使用 $release-checklist 完成当前明确范围内的工作。"

policy:
  allow_implicit_invocation: false
