interface:
  display_name: "skill-test"
  short_description: "Validate skill files for structural compliance and behavioral correctness. Three modes: static (linter), spec (behavioral), audit (coverage report). 当用户明确点名该流程或当前任务与其专业目标直接匹配时使用；不用"
  default_prompt: "使用 $skill-test 完成当前明确范围内的工作。"

policy:
  allow_implicit_invocation: false
