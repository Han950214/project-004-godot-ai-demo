interface:
  display_name: "reverse-document"
  short_description: "Generate design or architecture documents from existing implementation. Works backwards from code/prototypes to create missing planning docs. 当用户明确点名该流程或当前任务与其专业目标直接匹配时使用；不用于无关的一般开"
  default_prompt: "使用 $reverse-document 完成当前明确范围内的工作。"

policy:
  allow_implicit_invocation: false
