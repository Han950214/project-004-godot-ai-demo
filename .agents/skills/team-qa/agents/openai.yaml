interface:
  display_name: "team-qa"
  short_description: "Orchestrate the QA team through a full testing cycle. Coordinates qa-lead (strategy + test plan) and qa-tester (test case writing + bug reporting) to produce a complete QA package "
  default_prompt: "使用 $team-qa 完成当前明确范围内的工作。"

policy:
  allow_implicit_invocation: false
