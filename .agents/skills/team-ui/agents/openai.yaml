interface:
  display_name: "team-ui"
  short_description: "Orchestrate the UI team through the full UX pipeline: from UX spec authoring through visual design, implementation, review, and polish. Integrates with $ux-design, $ux-review, and "
  default_prompt: "使用 $team-ui 完成当前明确范围内的工作。"

policy:
  allow_implicit_invocation: true
