interface:
  display_name: "bug-triage"
  short_description: "Read all open bugs in production/qa/bugs/, re-evaluate priority vs. severity, assign to sprints, surface systemic trends, and produce a triage report. Run at sprint start or when t"
  default_prompt: "使用 $bug-triage 完成当前明确范围内的工作。"

policy:
  allow_implicit_invocation: true
