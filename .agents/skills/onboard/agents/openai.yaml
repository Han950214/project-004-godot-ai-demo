interface:
  display_name: "onboard"
  short_description: "Generates a contextual onboarding document for a new contributor or agent joining the project. Summarizes project state, architecture, conventions, and current priorities relevant "
  default_prompt: "使用 $onboard 完成当前明确范围内的工作。"

policy:
  allow_implicit_invocation: false
