interface:
  display_name: "estimate"
  short_description: "Estimates task effort by analyzing complexity, dependencies, historical velocity, and risk factors. Produces a structured estimate with confidence levels. 当用户明确点名该流程或当前任务与其专业目标直接匹配"
  default_prompt: "使用 $estimate 完成当前明确范围内的工作。"

policy:
  allow_implicit_invocation: false
