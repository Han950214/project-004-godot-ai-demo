interface:
  display_name: "qa-plan"
  short_description: "Generate a QA test plan for a sprint or feature. Reads GDDs and story files, classifies stories by test type (Logic/Integration/Visual/UI), and produces a structured test plan cove"
  default_prompt: "使用 $qa-plan 完成当前明确范围内的工作。"

policy:
  allow_implicit_invocation: false
