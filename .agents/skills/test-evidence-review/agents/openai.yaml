interface:
  display_name: "test-evidence-review"
  short_description: "Quality review of test files and manual evidence documents. Goes beyond existence checks — evaluates assertion coverage, edge case handling, naming conventions, and evidence comple"
  default_prompt: "使用 $test-evidence-review 完成当前明确范围内的工作。"

policy:
  allow_implicit_invocation: true
